package io.muehlbachler.bswe;

import org.springframework.stereotype.Service;
/**
 * Service class for string manipulation operations.
 */
// FIXME: add javadoc
@Service
public class StringService {
    /**
     * Converts the given string to uppercase.
     *
     * @param string the input string to convert; if null, will return null
     * @return the uppercase version of the input string; if the input is null, returns null
     */
    
    public String toUpperCase(final String string) {
        // Implement the method to convert the string to uppercase
        return string;
    }
}
